<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:solutions, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:solutions, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :solution, url(:solutions, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'solutions/form', :locals => { :f => f, :assignments => @assignments } %>
 <% end %>
</div>
